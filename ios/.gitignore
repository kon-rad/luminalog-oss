# macOS
.DS_Store

# Xcode project is generated by xcodegen — never commit it
*.xcodeproj

# Xcode user state & build artifacts
xcuserdata/
DerivedData/
build/
.build/

# Swift Package Manager
.swiftpm/

# Secrets — Firebase config is never committed (app runs in demo mode without it)
GoogleService-Info.plist

# Local per-developer build settings (e.g. GOOGLE_REVERSED_CLIENT_ID).
# The .example template is committed; the real file is not.
LuminaLog/Local.xcconfig

# Keep dependency pins
!LuminaLog.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

ios-luminalog/AuthKey_CA52C5LYT3.p8
AuthKey_CA52C5LYT3.p8



# Brainstorming visual companion scratch
.superpowers/
