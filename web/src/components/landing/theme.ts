/**
 * The immersive landing page's dark palette. Mirrors the exact hex values
 * already defined under `html.dark` in `src/app/globals.css` so the page
 * stays visually consistent with the rest of the app's dark theme, without
 * depending on the `html.dark` class actually being toggled (this page is
 * unconditionally dark, matching the Apple product-page template it draws
 * from, regardless of the visitor's site-wide theme preference).
 */
export const INK = '#16130E'
export const SURFACE = '#221E18'
export const CREAM = '#F3EEE4'
export const MUTE = '#A89E8F'
export const ACCENT = '#E5A063'
export const ACCENT_DEEP = '#CE7F44'
export const HAIRLINE = 'rgba(255,240,220,0.08)'
