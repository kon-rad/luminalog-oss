# Backend base URL for the /api/soul proxy (server-side only). Default: https://api.luminalog.com
API_URL=https://api.luminalog.com

# RevenueCat Web Billing (public API key rcb_...) — from RevenueCat dashboard, Task 0 ⬅
NEXT_PUBLIC_RC_WEB_BILLING_KEY=
# Master switch for web checkout UI; keep false until the founding launch is live
NEXT_PUBLIC_BILLING_ENABLED=false
